---
import Navigation from '../components/Navigation.astro';

interface Props {
  title?: string;
  hideNav?: boolean; // Allows bypassing navigation on public screens or root landings
}
const { title = "JSOS Core Control", hideNav = false } = Astro.props;
---
<!doctype html>
<html lang="en-GB" class="bg-brand-canvas text-slate-100 antialiased selection:bg-brand-accent/30">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{title}</title>
  </head>
  <body class="min-h-screen font-sans flex flex-col">
    { !hideNav && <Navigation /> }
    <div class="flex-1">
      <slot />
    </div>
  </body>
</html>
